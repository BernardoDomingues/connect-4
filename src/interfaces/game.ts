export interface PlayersDataInterface {
  playerOneData: {
    name: string;
    color: string;
  },
  playerTwoData: {
    name: string;
    color: string;
  }
};

export interface PlayerNameAndColorInterface {
  1: string;
  2: string;
};